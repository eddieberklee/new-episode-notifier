<h1>Notifier#check</h1>
<p>Find me in app/views/notifier/check.html.erb</p>

Episodes known to be released on CrunchyRoll: <%= @knownEpisodes.map(&:number).sort %>

<% @newEpisodes.each do |e| %>
  New episode <%= e %>!
<% end %>
